import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'AI Study Tools | StudyCo', description: 'Explore StudyCo AI Assistant, notes generation, document intelligence, and smart recommendations.' };

export default function AiLayout({ children }: { children: React.ReactNode }) { return children; }
