import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'AI Notes Generator | StudyCo', description: 'Generate notes, summaries, flashcards, and quizzes with StudyCo AI.' };

export default function GeneratorLayout({ children }: { children: React.ReactNode }) { return children; }
