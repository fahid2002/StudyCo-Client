import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'AI Document Intelligence | StudyCo', description: 'Analyze study PDFs, DOCX files, and TXT notes with StudyCo AI.' };

export default function DocumentLayout({ children }: { children: React.ReactNode }) { return children; }
