import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Privacy Policy | StudyCo', description: 'Read the StudyCo privacy policy and data handling information.' };

export default function PrivacyLayout({ children }: { children: React.ReactNode }) { return children; }
