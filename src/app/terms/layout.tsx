import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Terms of Service | StudyCo', description: 'Review the terms for using the StudyCo platform.' };

export default function TermsLayout({ children }: { children: React.ReactNode }) { return children; }
