import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Add a Study Session | StudyCo', description: 'Publish a new peer study session on StudyCo.' };

export default function AddSessionLayout({ children }: { children: React.ReactNode }) { return children; }
