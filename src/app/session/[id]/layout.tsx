import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Session Details | StudyCo', description: 'Review session information, ratings, availability, and booking options on StudyCo.' };

export default function SessionLayout({ children }: { children: React.ReactNode }) { return children; }
